# MongoDB Configuration
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/panel-bahai?retryWrites=true&w=majority&authSource=admin

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=tu-clave-secreta-muy-segura

# JWT Configuration
# IMPORTANTE: En producción, debe tener mínimo 32 caracteres
# Generar con: openssl rand -base64 32
JWT_SECRET=tu-jwt-secret-muy-seguro-minimo-32-caracteres-en-produccion

# Rate Limiting (Upstash Redis)
# Obtener en: https://console.upstash.com/
# Opcional: Si no se configura, se usa memoria (solo desarrollo)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Admin User Configuration
ADMIN_EMAIL=admin@panel-bahai.org
ADMIN_PASSWORD=tu-password-seguro

# Optional: Development Domain
# DEV_DOMAIN=panel-bahai.local